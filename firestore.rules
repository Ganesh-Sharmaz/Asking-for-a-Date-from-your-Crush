rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // All reads and writes go through the Vercel Admin SDK API.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
